import {StyleSheet} from 'react-native'
import { COLORS } from '../../utils/colors'
export const styles = {
    mainButton:{ width: '100%', height: 65, justifyContent: 'center', alignItems: 'center', backgroundColor: COLORS.PRIMARYPURPLE, marginVertical: '5%', borderRadius: 15 },
    btnText:{ fontSize: 26, color: COLORS.WHITE, fontWeight: 'bold' },
}